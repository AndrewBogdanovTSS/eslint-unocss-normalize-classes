#!/bin/sh
# Not a gate, a reminder. The full run lives in CI; this catches the cheap
# failures - a type error, a README that drifted - before they cost a round trip.
#
# Install with: pnpm hooks:install
set -e
pnpm typecheck
pnpm check:docs
